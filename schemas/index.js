const loginSchema = require('./loginSchema');
const registerSchema = require('./registerSchema');
const petSchema = require('./registerSchema');

module.exports = {
    loginSchema,
    registerSchema,
    petSchema,
};
